# 🏠 Прогнозирование стоимости квартир с использованием нейронной сети

Проект для классификации домов по ценовым категориям на основе их характеристик с использованием глубокого обучения.

## 📋 Описание

Система машинного обучения, которая анализирует характеристики квартир (площадь, количество комнат, этажей, год постройки) и классифицирует их по трем ценовым категориям (дешево, средне, дорого) с помощью нейронной сети на базе TensorFlow/Keras.

## ✨ Особенности

- **Реалистичная генерация данных** с учетом зависимостей между параметрами
- Автоматическая классификация домов по ценовым категориям (3 класса)
- Нормализация признаков с помощью StandardScaler
- Нейронная сеть с архитектурой глубокого обучения
- Сохранение обученной модели и scaler для дальнейшего использования
- Предсказания на новых данных с выводом вероятностей для каждого класса
- Стратифицированное разделение данных для сохранения пропорций классов

## 🛠 Технологии

- **Python 3.7+**
- **TensorFlow/Keras** - для построения и обучения нейронной сети
- **scikit-learn** - для предобработки данных
- **pandas** - для работы с данными
- **numpy** - для численных операций
- **pickle** - для сохранения scaler

## 📦 Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ваш-username/house-price-prediction.git
cd house-price-prediction
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

Или вручную:
```bash
pip install numpy pandas scikit-learn tensorflow
```

## 🚀 Использование

### Базовое использование

Запустите основной скрипт:
```bash
python house_price_prediction.py
```

### Что делает скрипт:

1. **Генерация данных**: Создает синтетический датасет из 1000 домов с реалистичными характеристиками:
   - **Площадь**: зависит от количества комнат (60-150 м²)
   - **Количество комнат**: 2-5
   - **Количество этажей**: 1-3
   - **Год постройки**: 1980-2024
   - **Цена**: рассчитывается с учетом всех факторов (площадь × базовая цена × множитель этажности × фактор возраста + шум)

2. **Предобработка**:
   - Разделение цен на 3 класса по квантилям (33%, 66%)
   - Нормализация признаков (StandardScaler)
   - Стратифицированное разделение на обучающую (60%) и тестовую (40%) выборки

3. **Обучение модели**:
   - Архитектура: 16 → 8 → 3 нейрона
   - Функция активации: ReLU для скрытых слоев, Softmax для выходного
   - Оптимизатор: Adam
   - Loss: sparse_categorical_crossentropy
   - Эпохи: 20
   - Batch size: 16

4. **Сохранение модели**:
   - Модель сохраняется в `house_price_model.h5`
   - Scaler сохраняется в `house_scaler.pkl`

5. **Предсказания**:
   - Классификация 5 примеров новых домов
   - Вывод вероятностей для каждого класса

## 📊 Архитектура модели

```
Входной слой (4 признака: площадь, комнаты, этажи, год постройки)
    ↓
Скрытый слой 1 (16 нейронов, ReLU)
    ↓
Скрытый слой 2 (8 нейронов, ReLU)
    ↓
Выходной слой (3 нейрона, Softmax)
    ↓
Классы: [0: дешево, 1: средне, 2: дорого]
```

## 📈 Пример вывода

После запуска вы увидите:

```
Epoch 1/20
38/38 [==============================] - 1s 2ms/step - loss: 0.9234 - accuracy: 0.5217
...
Epoch 20/20
38/38 [==============================] - 0s 2ms/step - loss: 0.3456 - accuracy: 0.8567

Точность: 0.85

Модель и scaler сохранены!

==================================================
ПРЕДСКАЗАНИЯ НА НОВЫХ ДАННЫХ
==================================================

Результаты предсказаний:
--------------------------------------------------

Дом 1:
  Площадь: 70.0 м²
  Комнат: 2
  Этажей: 1
  Год постройки: 2010
  Предсказанный класс: 0 (дёшево)
  Вероятности:
    - дёшево: 65.23%
    - средне: 30.12%
    - дорого: 4.65%

...
```

## 💾 Использование сохраненной модели

После обучения вы можете загрузить модель и использовать её для предсказаний:

```python
from tensorflow.keras.models import load_model
import pickle
import numpy as np

# Загрузка модели и scaler
model = load_model('house_price_model.h5')
with open('house_scaler.pkl', 'rb') as f:
    scaler = pickle.load(f)

# Новые данные для предсказания
new_house = np.array([[80.0, 3, 2, 2015]])

# Нормализация
new_house_scaled = scaler.transform(new_house)

# Предсказание
prediction = model.predict(new_house_scaled, verbose=0)
predicted_class = np.argmax(prediction, axis=1)[0]

class_names = {0: 'дёшево', 1: 'средне', 2: 'дорого'}
print(f'Предсказанный класс: {predicted_class} ({class_names[predicted_class]})')
```

## 📁 Структура проекта

```
house-price-prediction/
│
├── house_price_prediction.py    # Основной скрипт с кодом
├── house_price_model.h5         # Сохраненная модель (после запуска)
├── house_scaler.pkl              # Сохраненный scaler (после запуска)
├── README.md                     # Этот файл
├── requirements.txt              # Зависимости проекта
└── .gitignore                    # Игнорируемые файлы Git
```

## 🔧 Параметры модели

Вы можете настроить следующие параметры в коде:

- `n` - количество образцов в датасете (по умолчанию: 1000)
- `test_size` - размер тестовой выборки (по умолчанию: 0.4)
- `epochs` - количество эпох обучения (по умолчанию: 20)
- `batch_size` - размер батча (по умолчанию: 16)
- Количество нейронов в скрытых слоях (по умолчанию: 16, 8)
- Квантили для разделения на классы (по умолчанию: 0.33, 0.66)

## 📊 Логика ценообразования

Модель учитывает следующие факторы при расчете цены:

- **Базовая цена за м²** зависит от количества комнат:
  - 2 комнаты: 100,000 руб/м²
  - 3 комнаты: 120,000 руб/м²
  - 4 комнаты: 140,000 руб/м²
  - 5 комнат: 160,000 руб/м²

- **Множитель этажности**: многоэтажные дома дороже (+15% за каждый дополнительный этаж)

- **Фактор возраста**: более новые дома дороже (учитывается год постройки)

- **Случайный шум**: добавляет реалистичную вариативность

## 📝 Лицензия

MIT License

## 👤 Автор

Ваше имя

## 🤝 Вклад в проект

Вклады приветствуются! Если у вас есть предложения по улучшению, создайте issue или pull request.

## 📧 Контакты

Если у вас есть вопросы, создайте issue в репозитории.

---

⭐ Если проект вам понравился, поставьте звезду!

